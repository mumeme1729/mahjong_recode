import React from 'react'
import { PROPS_GAME_RESULTS, PROPS_GAME_RESULTS_SCORE } from '../types'

const ScoreDetail:React.FC<any> = (result) => {
    
    
    return (
        <div>
            <div>
                月ごとの合計スコア
                
            </div>
        </div>
    )
}

export default ScoreDetail
